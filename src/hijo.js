
function Hijo ({ incrementar }) {

    return(
        <div>
            <h1>Hola soy tu hijo</h1>
            <button onClick= {() => incrementar () }>presioname</button>
        </div>
    )
}

export default  Hijo;